# このプロジェクトについて

漢字の筆順を1画ずつアニメーション表示するWebアプリ。ビルドツールもnpm依存も使わず、HTML / CSS / 素のJavaScriptのみで動く。

## 前提

- 起動は `npx serve .`（`file://` では動かない）
- 筆順データは KanjiVG のSVG。`kanjivg/kanji/<文字コード16進5桁>.svg` を fetch して読む。例：妻 U+59BB → `059bb.svg`
- SVG内の `<path>` は書き順どおりに並んでいる。`d` 属性を順に取り出し、`stroke-dasharray` / `stroke-dashoffset` を requestAnimationFrame で動かして描き進めている
- 過去に hanzi-writer + hanzi-writer-data-jp を試したが、後者はデータが同梱されておらず使えなかった。戻さないこと

## 編集時のルール

- npmパッケージを追加しない。依存ゼロを保つ
- 色は `styles.css` の `:root` にあるCSS変数（和紙・墨・藍・朱）を使い、新しい色を直書きしない
- 文字サイズ・余白はスマートフォンの縦画面（幅420px以下）を基準にする
- ボタンのラベルは「再生」「1画進む」のように、押すと何が起きるかがそのまま分かる日本語にする
- データ取得に失敗したときは必ず画面上に理由とパスを出す（無言で空白にしない）

## 変更履歴

- 初版: hanzi-writer構成（データ不備のため廃止）
- 現行: KanjiVG直読み。表示 / 再生 / 1画進む / なぞり / 速さ / 熟語のタブ切り替え
